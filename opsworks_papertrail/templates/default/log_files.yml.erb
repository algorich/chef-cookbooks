files:
  <% @log_files['rails'].each do |path| %>
  - <%= "#{@deploy[:deploy_to]}/#{path}" %>
  <% end %>
  <% @log_files['common'].each do |path| %>
  - <%= path %>
  <% end %>

destination:
  host: <%= @host %>
  port: <%= @port %>
