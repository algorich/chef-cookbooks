files:
  - <%= @deploy[:deploy_to] %>/log/production.log
  - <%= @deploy[:deploy_to] %>/log/unicorn.stderr.log
  - <%= @deploy[:deploy_to] %>/log/unicorn.stdout.log

destination:
  host: logs.papertrailapp.com
  port: <%= @port %>
